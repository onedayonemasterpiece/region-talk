# Research intake

Place reviewed files only under the exact pattern:

`region-talk-external-research-result-<request-id>.json`

PR validation is read-only. Merge to trusted `main` opens an all-or-nothing SQLite import. See `docs/research-intake.md` and `schemas/research-intake.schema.json`.
